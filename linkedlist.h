#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

/*
// start of structs//
struct CardList {
    char suit;
    char value;
    struct CardList *temp head;
    struct CardList *next;
    struct CardList *prev;
};

typedef struct {
    struct CardList *head;
};
*/